<% if @feed_items.any? %>
  <ol class="microposts">
    <!-- Rails knows to look for the class view of the object that it is rendering i.e app/views/microposts/_micropost -->
    <%= render @feed_items %>
  </ol>
  <%= will_paginate @feed_items %>
<% end %>